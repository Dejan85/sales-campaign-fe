import { colors } from './colors'
import { widths } from './elements-sizes'
import { gap, margins, paddings } from './elements-spacings'
import { fonts, fontsSizes, fontWeights } from './fonts'
import { letterSpacings, lineHeights } from './text-spacings'

export { colors, widths, margins, paddings, gap, fonts, fontsSizes, fontWeights, letterSpacings, lineHeights }
