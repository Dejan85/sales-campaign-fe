import { css } from '@emotion/react'

export const primary = css`
	display: flex;
	flex-direction: column;
	max-width: var(--mwd-650-px);
	width: var(--wd-100-per);
	border: none;
	background-color: var(--primary-color);
	/* padding: var(--pd-tl-20); */
`
